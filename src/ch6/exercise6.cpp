#include <iostream>
#include <vector>
#include <string>
#include <cctype>
using namespace std;
bool lengthCompare(const string &s1, const string &s2){
    return (s1.size() < s2.size())? true : false;
}

int main(void)
{
    cout<<"end"<<endl;
    
    return 0;
}